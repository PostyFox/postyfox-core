# Catch-all (core-only default): everything not matched by an explicit route above goes to
# core-api. A frontend deployment overrides THIS file (same conf.d path) to point the root at
# its SPA container instead — see postyfox-frontend/deploy.
location / { proxy_pass http://core; }
