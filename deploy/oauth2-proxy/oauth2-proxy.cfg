# Example oauth2-proxy config fronting core-api. Adjust upstream/redirect per environment.
http_address = "0.0.0.0:4180"
provider = "oidc"
oidc_issuer_url = "http://keycloak:8080/realms/PostyFox"
client_id = "oauth2-proxy"
client_secret = "oauth2-proxy-secret"
cookie_secret = "0123456789abcdef0123456789abcdef"
email_domains = ["*"]
upstreams = ["http://core-api:8080"]
redirect_url = "http://localhost:4180/oauth2/callback"
# Inject the identity header the APIs trust (Auth__UserHeader).
pass_access_token = true
set_xauthrequest = true
pass_user_headers = true
skip_provider_button = true
